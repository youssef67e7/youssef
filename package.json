{
  "name": "pharmaworld",
  "version": "1.0.0",
  "private": true,
  "description": "PharmaWorld - Enterprise Pharmacy Management System",
  "scripts": {
    "build:admin": "cd admin-web && npm install && npm run build",
    "build:backend": "cd backend && npm install && npm run build",
    "build:admin+backend": "npm run build:admin && npm run build:backend",
    "build:copies": "node scripts/copy-builds.js",
    "build": "npm run build:admin+backend && node scripts/copy-builds.js",
    "deploy:landing": "cd landing-page && vercel --prod",
    "deploy:landing:preview": "cd landing-page && vercel",
    "install:all": "npm install && cd admin-web && npm install && cd ../backend && npm install"
  }
}
