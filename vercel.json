{
  "version": 2,
  "installCommand": "npm install && cd admin-web && npm install && cd ../backend && npm install",
  "buildCommand": "cd admin-web && npm run build && cd ../backend && npm run build",
  "outputDirectory": "admin-web/dist",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
